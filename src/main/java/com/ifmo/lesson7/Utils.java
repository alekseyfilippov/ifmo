package com.ifmo.lesson7;

import com.ifmo.lesson6.List;

public class Utils {
    // public static Object find(List list, Predicate P)
    // interface Predicate (Предикат - это имя интерфейса)
    // boolean test(Object o); находим первый, который возр. True
    public static Object find(List list, Predicate predicate) {
        return null;
    }
    // for (Object o : o)
}
